#include<stdio.h>
#include<string.h>
printreverse(char *);
void main()
{
	char str[]="I AM A GEEK";
	printreverse(str);
}

printreverse(char s[])
{
	int i=0,n= strlen(s);
	for(i=n-1;i >=0;i--)
	{
		if(s[i]==' ')
	{
		s[i]='\0';
		printf("%s ", &(s[i]) + 1);
	}
	}
	printf("%s", s);
}
